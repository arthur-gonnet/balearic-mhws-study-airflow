numpy<2.4 # marineHeatWaves.py relies on an implicit array-to-scalar conversion that numpy hard-errors on from 2.4+
pandas
xarray>=2025.6 # older versions lack Dataset.to_zarr(zarr_format=...)/Dataset.drop_encoding(), both required by data/io.py
dask
scipy
zarr # the codebase always writes Zarr format 2 explicitly - any zarr-python major version works

# For plotting
matplotlib
cartopy
geopy
shapely
cmocean
roman # used for some figure labelling
pymannkendall

# For downloading the data
copernicusmarine

# For submitting/polling Slurm jobs from Airflow
apache-airflow-providers-ssh